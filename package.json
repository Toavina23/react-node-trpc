{
  "name": "trpc-react",
  "version": "1.0.0",
  "description": "a template for trpc node react fullstack app",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specfied\" && exit 1",
    "dev:server": "tsx watch server/src",
    "dev:client": "cd ./client && vite",
    "dev": "run-p dev:* --print-label",
    "start": "node dist/index.js",
    "build": "tsc && cd ./client && npm run build"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.0",
    "npm-run-all": "^4.1.5",
    "tsx": "^4.1.1",
    "typescript": "^5.2.2",
    "wait-port": "^1.1.0"
  }
}
